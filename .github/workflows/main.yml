name: LateX to PDF

on:
    push:
        branches: 
        - main
    pull_request:
        branches: [ main ]
# jobs:
#   build_latex:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Set up Git repository
#         uses: actions/checkout@v2
#       - name: latex2pdf
#         id: compile-latex-document
#         uses: thomas-chauvet/latex2pdf-action@1.1.0
#         with:
#           output_dir: output
#           main_latex_file: uiophdthesis-main.tex
#           ctan_packages: amsmath amsfonts lua-uni-algos
#       - name: Upload PDF to the workflow tab
#         id: upload-workflow-tab
#         uses: actions/upload-artifact@v2
#         with:
#           name: output
#           path: uiophdthesis-main.pdf

jobs:
  build_latex:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v4
      - name: Compile LaTeX document
        uses: AffineParameter/latex-action@a0.2.1
        with:
          root_file: uiophdthesis-main.tex
      - name: Upload PDF to the workflow tab
        id: upload-workflow-tab
        uses: actions/upload-artifact@v4
        with:
            name: output
            path: output/uiophdthesis-main.pdf
