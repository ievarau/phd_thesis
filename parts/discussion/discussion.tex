

\chapter{Discussion}

Summary of the thesis in a broader context.





    \section{The current status of computational models for TF-DNA interactions}

    PWM limitations

    Previous attempts to evaluate modeling approaches for TF sequence specificity. DREAM5 challenge.
    There are comparisons of models. For example in this paper:
    % https://www.nature.com/articles/nbt.2486 
    Figure 2 has the comparison heatmap. Maybe I could use that figure. Most of the models are PWM, but there are dinucleotide and kmer as well. No deep learning?
    Performance of the models and their implementations vary and depend on the TF binding variance \cite{weirauch2013evaluation}.

    This benchmark as well:
    % https://link.springer.com/article/10.1186/s13059-020-01996-3


    The most recent challenge, Ibis, aims to engage the scientific community into thinking about and implement new TF-DNA interaction models.

    Discuss motif trimming in JASPAR 2024. Apparently Taipale's selex paper or whatever says that low information positions are the ones that matter when it comes to the binding of a specific TF family member (why one member and not another will bind even though the motifs are the same). I could give an argument for downstream data analysis, that those positions don't really provide any information. Also, maybe cooperation matters more and those low information positions are actually masking the possible grammar, because of technical limitations. You chip only one protein and expect short, so surrounding are kind of averaged out.
    Brings the conformation of the DNA. (dna shape paper from Anthony)


    Limitations of PFMs in all major TF-DNA binding models repositories - DNA methylation is not represented.


    Deep learning.


    \section{Identifying and defining TF co-binding}



Evolutionary conservation. More conservation of the original tfbs when there is a co-binder.




Species conservation.



    Future work - cell type specificity. I can discuss that it is hard to look into, because of the lack of annotation. <---- This could mention that in the next session database maintenance importance will be discussed.


CTCF result. That it was thought to be another TF. Future work would be to understand and figure out what function that could be.





    \section{The need for maintenance and update of the scientific databases}


    Ease of use - web interfaces are good, but limited. Usually, they do not allow for large inputs, take time and downstream analysis of the results can be complicated due to the limited output (for example, often you cannot download the results as tables). The next thing to move to is containerization of command line tools. This would allow to use the tool in its full capacity, but in a much more user-friendly way. Arguably, containerized applications are easy to use for researchers with very limited experience in coding.


    FAIR principles

    Open access software and data


    "Research parasites" according to some researchers. But a lot of data remains understudied. Examples of open-access data, such as ENCODE, are excellent. Data can be used in multiple studies and keep producing the scientific insights. For example, DHS data from ENCODE was used by Viestra et al. to compute the DHS footprints. Since ENCODE data was also used in UniBind, thus for COBIND, I was able to reuse the footprints to analyse the footprints in matching cell types. So the experimental data is not meant to be analysed once with one particular pipeline. If researchers store it well with good annotations, data will be reused multiple times. However, the need for proper annotation should be emphasized here. In particular, cell type annotations. We should come up with consensus annotations, so data from multiple experiments could be easily integrated together.
    There are some cell type annotations available, but it is still challenging to apply them to large collections of data. 
    BRENDA is tissue type ontology. Hard to automatically annotate large numbers of datasets. Needs manual interference to achieve best results.

    Manual curation of the data
    It is often underappreciated how much manual labor might be needed to maintain the quality of the data. Nowadays with a variety of computational tools, a lot of such labor can be eased, but often the final say comes from a researcher. For example, for JASPAR database update motifs are prepared using a computational pipeline. Top 3 motifs that are selected for the curation can have the same centrality values, so there is no automatic way to select the best of them. And centrality is only one criteria. Thus, the curator then evaluates the motifs visually and searchers the literature to select the best, if any of them are good at all.

    Another example of the effort to increase the quality of the data in databases is revising and refining the classification of the transcription factors. It is a separate effort that many might not see as needed. For 2024 release plant TF classification was created and we were able to use this new classification and update the metadata for all plant transcription factors in JASPAR. Unfortunately, but for other taxons, such as nematodes or insects, we have to rely on unsystematic annotations to assign TF classes and families.